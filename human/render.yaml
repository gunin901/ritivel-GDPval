# See /render.yaml at repo root (preferred).
services:
  - type: web
    name: gdpval-human-eval
    runtime: docker
    plan: starter
    dockerfilePath: ./human/ui/Dockerfile
    dockerContext: .
    healthCheckPath: /login
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: DATA_DIR
        value: /var/data
      - key: MEDIA_BACKEND
        value: s3
      - key: NODE_ENV
        value: production
    disk:
      name: gdpval-data
      mountPath: /var/data
      sizeGB: 5
